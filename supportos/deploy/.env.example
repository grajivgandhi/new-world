# Copy to .env and replace every placeholder before starting services.
# Never commit the real .env file.

SECRET_KEY_BASE=REPLACE_WITH_A_LONG_RANDOM_SECRET
POSTGRES_PASSWORD=REPLACE_WITH_A_LONG_RANDOM_PASSWORD
REDIS_PASSWORD=REPLACE_WITH_A_LONG_RANDOM_PASSWORD

FRONTEND_URL=https://support.example.com
FORCE_SSL=true
RAILS_ENV=production
NODE_ENV=production
INSTALLATION_ENV=docker

# Generate a strong secret; do not reuse passwords from other services.
# Configure SMTP before enabling customer email support.
