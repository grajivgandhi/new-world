services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: leadrelay
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d leadrelay"]
      interval: 5s
      timeout: 5s
      retries: 10
